#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int target,flag=0,i;
    scanf("%d",&target);
    for(i=0;i<n;i++){
        if(target==arr[i]){
            flag=1;
        break;
        }
    }
    
    if(flag==1)
        printf("Found");
    else
        printf("Not Found");
    
    return 0;
}
